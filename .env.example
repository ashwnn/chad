### Copy this file to .env and fill in the values before running `docker-compose up` ###

# Discord Bot Token (required to run the bot service)
DISCORD_BOT_TOKEN=

# Grok API key (required for AI chat functionality)
GROK_API_KEY=

# Gemini API key (required for /search command with grounded search)
GEMINI_API_KEY=
GEMINI_MODEL=

# Grok API base URL (default)
GROK_API_BASE=https://api.x.ai/v1
GROK_CHAT_MODEL=grok-beta

# Database path inside the container (mapped to ./data on host)
DATABASE_PATH=/data/chad.sqlite3

# Prompt limits
MAX_PROMPT_CHARS=4000

# Web service host/port
WEB_HOST=0.0.0.0
WEB_PORT=8000

# CORS and API rate limiting
CORS_ORIGINS=*
API_RATE_LIMIT_PER_MINUTE=120

# Logging (optional rotating file handler)
LOG_FILE=/data/logs/chad.log
LOG_MAX_BYTES=1048576
LOG_BACKUP_COUNT=5